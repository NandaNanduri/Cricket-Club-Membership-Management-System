const MemberDashboard = () => {
    return (
        <div>
            <h1>Welcome, Club Member!</h1>
            <p>You have access to general announcements and member-only events.</p>
        </div>
    );
};

export default MemberDashboard;
